doctype html

html
    head
        meta(charset='uft-8')
        title RSS-AGENT

    body
        h1 Welcome to the feed-list

        ul
            each feedData in feeds
                a(href = feedData.url)
                    li= feedData.name

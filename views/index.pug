extends layout.pug

block body
    a(href='/feed-form') Add form
    h1 Welcome to the feed-list

    ul
        each feedData in feeds
            a(href = feedData.url)
                li= feedData.name
